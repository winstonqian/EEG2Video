\documentclass[11pt]{article}

% ── Core packages ────────────────────────────────────────────
\usepackage[margin=1in]{geometry}
\usepackage{amsmath, amssymb}   % math + \checkmark
\usepackage{booktabs}           % \toprule \midrule \bottomrule
\usepackage{graphicx}           % \includegraphics
\usepackage{hyperref}           % links
\usepackage{parskip}            % paragraph spacing
\usepackage{microtype}          % better typography
\usepackage{natbib}             % \cite

\hypersetup{colorlinks=true, linkcolor=blue, urlcolor=blue, citecolor=blue}

\title{\textbf{LATA: Latency-Aware Temporal Alignment}\\
\large Section draft for team NeurIPS report\\
\normalsize MAS.S60 / 6.S985 --- Multimodal AI --- Spring 2026}
\author{Rachel Li}
\date{}

\begin{document}
\maketitle
\tableofcontents
\newpage

% ─────────────────────────────────────────────────────────────
\section{Proposed Method: Latency-Aware Temporal Alignment}
% ─────────────────────────────────────────────────────────────

\subsection{Motivation}

Standard cross-attention assumes that time $t$ in modality $A$ corresponds
strictly to time $t$ in modality $B$.
This assumption is physically incorrect for brain signals: neural responses
always \emph{lag} the driving stimulus by a biological transit delay
$\delta$ — early visual responses (P100) peak at $\sim$100\,ms
post-stimulus, while higher-level semantic responses (P300) peak at
$\sim$300\,ms~\cite{luck2014introduction}.
Existing EEG-to-video alignment methods, including the EEG2Video
baseline~\cite{liu2024eeg2video}, ignore this latency entirely, aligning
the full EEG clip to a single global video embedding and making it
impossible to track within-clip temporal dynamics.

We propose \textbf{LATA} (Latency-Aware Temporal Alignment), a
plug-and-play PyTorch module that replaces standard cross-attention with a
biologically-motivated variant that \emph{learns} the transit delay
$\delta$ end-to-end from data, with no manual tuning or supervision on
$\delta$ itself.

% ─────────────────────────────────────────────────────────────
\subsection{Module Design}
% ─────────────────────────────────────────────────────────────

Let $\mathbf{E} = [\mathbf{e}_1, \ldots, \mathbf{e}_K] \in \mathbb{R}^{K
\times d}$ denote the $K$ temporal chunks of an EEG clip and
$\mathbf{V} = [\mathbf{v}_1, \ldots, \mathbf{v}_K] \in \mathbb{R}^{K \times
d}$ the corresponding video chunk embeddings.
LATA maintains a trainable logit vector
$\boldsymbol{\ell} \in \mathbb{R}^{\Delta+1}$ over candidate delays
$\delta \in \{0, 1, \ldots, \Delta\}$:
%
\begin{equation}
    \mathbf{w} = \mathrm{softmax}(\boldsymbol{\ell}),
    \qquad w_\delta \in [0,1],
    \quad \textstyle\sum_\delta w_\delta = 1.
    \label{eq:softmax}
\end{equation}
%
The \emph{latency-corrected} stimulus at chunk position $k$ is a soft
weighted sum over lagged video chunks:
%
\begin{equation}
    \tilde{\mathbf{v}}_k
    = \sum_{\delta=0}^{\Delta} w_\delta\, \mathbf{v}_{k-\delta},
    \label{eq:lc}
\end{equation}
%
where $\mathbf{v}_{k-\delta} = \mathbf{0}$ for $k < \delta$ (zero-padding).
When $\mathbf{w}$ is peaked at $\delta^*$,
$\tilde{\mathbf{v}}_k \approx \mathbf{v}_{k-\delta^*}$:
chunk $k$ of the EEG is matched against the stimulus content from
$\delta^*$ steps earlier — precisely the biological lag.
LATA then computes multi-head cross-attention with queries from the neural
stream and keys/values from the latency-corrected stimulus:
%
\begin{equation}
    \mathrm{LATA}(\mathbf{E}, \mathbf{V})
    = \mathrm{MultiHeadAttn}\!\bigl(
        \mathbf{Q}{=}\mathbf{W}_q\mathbf{E},\;
        \mathbf{K}{=}\mathbf{W}_k\tilde{\mathbf{V}},\;
        \mathbf{V}{=}\mathbf{W}_v\tilde{\mathbf{V}}
      \bigr).
    \label{eq:lata}
\end{equation}

\paragraph{Training objective.}
We train LATA with a latency-aware InfoNCE loss.
After latency correction, neural chunk $k$ should be maximally similar to
the corresponding stimulus chunk $k$ (positive pair) and dissimilar to all
other chunk positions in the batch (negatives):
%
\begin{equation}
    \mathcal{L}_{\mathrm{LATA}}
    = -\frac{1}{BK}\sum_{i=1}^{BK}
      \log \frac{
        \exp\!\bigl(\mathrm{sim}(\mathbf{n}_i,\,\tilde{\mathbf{v}}_i)/\tau\bigr)
      }{
        \sum_{j=1}^{BK}
        \exp\!\bigl(\mathrm{sim}(\mathbf{n}_i,\,\tilde{\mathbf{v}}_j)/\tau\bigr)
      },
    \label{eq:infonce}
\end{equation}
%
where $\mathbf{n}_i$, $\tilde{\mathbf{v}}_i$ are L2-normalised chunk
encodings and $\tau$ is a temperature hyperparameter.
The gradient of~\eqref{eq:infonce} flows through
$\mathbf{w} = \mathrm{softmax}(\boldsymbol{\ell})$
differentiably via~\eqref{eq:lc}, driving the delay logits toward the
true biological lag without any explicit supervision on $\delta$.

\paragraph{Generality.}
LATA is modality-agnostic: the same module applies unchanged to
EEG$\leftrightarrow$Video (this work),
EEG$\leftrightarrow$Audio (auditory BCI),
fMRI$\leftrightarrow$Text (hemodynamic delay $\sim$6\,s), or
sEEG$\leftrightarrow$Robotics (motor BCI).
It requires no architectural changes beyond swapping the input tensors.
The learned distribution $\mathbf{w}$ is directly interpretable: after
training, $\mathbb{E}[\delta] = \sum_\delta \delta w_\delta$ gives the
expected lag in chunk steps, directly comparable to known ERP latencies.

% ─────────────────────────────────────────────────────────────
\section{Experiments}
% ─────────────────────────────────────────────────────────────

\subsection{Synthetic Validation}
\label{sec:lata-synthetic}

\paragraph{Setup.}
To validate that LATA correctly recovers an unknown biological delay from
noisy data — and to provide a modality-independent proof of generality —
we construct synthetic paired sequences with a \emph{known} ground-truth
delay $\delta_\mathrm{true}$:
%
\begin{equation}
    \mathbf{n}[k] = \mathbf{s}[k - \delta_\mathrm{true}] + \boldsymbol{\varepsilon},
    \qquad \boldsymbol{\varepsilon} \sim \mathcal{N}(\mathbf{0},\,\sigma^2\mathbf{I}),
\end{equation}
%
where $\mathbf{s}[k] \sim \mathcal{N}(\mathbf{0}, \mathbf{I})$ is a random
stimulus and $\sigma = 0.4$ models neural noise.
We evaluate four ground-truth delays $\delta_\mathrm{true} \in \{0,1,2,3\}$
with $K{=}8$ chunks per clip, $d{=}128$, $N{=}2048$ training samples, and
LATA searching over $\Delta{=}4$ candidate delays.
We train for 400 epochs with AdamW ($\mathrm{lr}=5\times10^{-4}$, cosine
annealing) and InfoNCE temperature $\tau=0.05$.

\paragraph{Results.}
LATA correctly identifies $\delta_\mathrm{true}$ in all four cases:
the argmax of $\mathbf{w}$ matches the ground truth exactly
(Table~\ref{tab:lata-synthetic}, Figure~\ref{fig:lata-synthetic}).

\begin{table}[h]
\centering
\caption{Synthetic validation: learned delay vs.\ ground truth.}
\label{tab:lata-synthetic}
\begin{tabular}{ccc}
\toprule
$\delta_\mathrm{true}$ & Peak of learned $\mathbf{w}$ & Correct? \\
\midrule
0 & 0 & \checkmark \\
1 & 1 & \checkmark \\
2 & 2 & \checkmark \\
3 & 3 & \checkmark \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]
    \centering
    \includegraphics[width=\linewidth]{lata/lata_synthetic_validation.png}
    \caption{
        Synthetic validation results.
        Each column corresponds to one ground-truth delay
        $\delta_\mathrm{true} \in \{0,1,2,3\}$.
        \textbf{Top row:} learned delay distribution $\mathbf{w}$
        (blue bars); red dashed line marks the ground truth.
        The argmax matches $\delta_\mathrm{true}$ in all four cases.
        \textbf{Bottom row:} InfoNCE training loss, confirming convergence.
    }
    \label{fig:lata-synthetic}
\end{figure}

\paragraph{Discussion.}
The expected delay $\mathbb{E}[\delta]$ is pulled toward the midpoint
($\approx 2.0$) because the distribution does not fully sharpen at
moderate SNR within 400 epochs.
However, the argmax — the delay the model \emph{commits to} — is correct
in all cases, confirming that the InfoNCE gradient reliably identifies the
true lag.
This experiment constitutes the \emph{generalizability proof} for LATA:
the module recovers the correct delay purely from the temporal structure
of paired sequences, with no supervision on $\delta$ and no
domain-specific assumptions.

% ─────────────────────────────────────────────────────────────
\subsection{Real EEG--Video Alignment on SEED-DV}
\label{sec:lata-seeddv}
% ─────────────────────────────────────────────────────────────

\paragraph{Setup.}
We apply LATA to all 20 subjects of the SEED-DV
dataset~\cite{liu2024eeg2video}.
For each subject, 6 sessions are used for training (1{,}400 clips) and
1 held-out session for validation (200 clips).
EEG clips (62 channels, 2\,s at 200\,Hz) are divided into $K{=}4$ chunks
of 100 timepoints (0.5\,s each) and encoded by a ShallowNet-style
temporal-spatial CNN projecting to $d_\mathrm{model}{=}128$.

Video features are CLIP ViT-B/32~\cite{radford2021clip} embeddings
extracted directly from the session MP4 files — one frame per 0.5\,s
chunk, shared across all subjects.
Frame timestamps follow the known experimental protocol
(3\,s hint + $5\times2$\,s clips per concept, 13\,s per concept block).
These chunk-level visual features provide genuine within-clip temporal
variation (mean cosine similarity between adjacent chunks $\approx 0.49$),
enabling the delay logits to receive gradient signal.
All subjects share the same video features; only the EEG encoder weights
differ across subjects.
We train each subject for 100 epochs with AdamW
($\mathrm{lr}=3\times10^{-4}$, cosine annealing),
batch size 64, InfoNCE temperature $\tau=0.07$, and $\Delta{=}3$.

\paragraph{Results.}
Table~\ref{tab:lata-seeddv} and Figure~\ref{fig:lata-seeddv} summarise
the learned delay distributions across all 20 subjects.
The results are highly consistent:
%
\begin{itemize}
    \item \textbf{14 of 20 subjects} (70\%) converge to a peak delay of
          $\delta^*{=}2$, corresponding to $\sim$810\,ms post-stimulus.
    \item \textbf{6 of 20 subjects} (30\%) converge to $\delta^*{=}1$
          ($\sim$500\,ms).
    \item No subject peaks at $\delta{=}0$ (no lag) or $\delta{=}3$
          (1500\,ms), confirming the learned delays are biologically
          plausible and not random.
\end{itemize}
%
The population mean expected delay is
$\mathbb{E}[\delta] = 1.58 \pm 0.05$ chunks $\approx \mathbf{790}$\,ms,
with remarkably low inter-subject variance (SD = 0.05 chunks = 25\,ms).
The mean learned distribution is
$\bar{\mathbf{w}} = [0.197,\; 0.268,\; 0.289,\; 0.246]$,
with the highest weight on $\delta{=}2$.

\begin{table}[h]
\centering
\caption{LATA on SEED-DV: per-subject learned peak delay and $\mathbb{E}[\delta]$.}
\label{tab:lata-seeddv}
\begin{tabular}{cccccc}
\toprule
Subject & $\delta^*$ & $\mathbb{E}[\delta]$ &
Subject & $\delta^*$ & $\mathbb{E}[\delta]$ \\
\midrule
 1 & 1 & 1.54 &  11 & 2 & 1.61 \\
 2 & 1 & 1.54 &  12 & 2 & 1.63 \\
 3 & 2 & 1.59 &  13 & 1 & 1.55 \\
 4 & 2 & 1.62 &  14 & 2 & 1.59 \\
 5 & 1 & 1.52 &  15 & 2 & 1.56 \\
 6 & 1 & 1.52 &  16 & 2 & 1.68 \\
 7 & 1 & 1.49 &  17 & 2 & 1.60 \\
 8 & 2 & 1.59 &  18 & 2 & 1.66 \\
 9 & 2 & 1.60 &  19 & 2 & 1.55 \\
10 & 2 & 1.54 &  20 & 2 & 1.70 \\
\midrule
\multicolumn{6}{c}{Mean $\mathbb{E}[\delta] = 1.58 \pm 0.05$ chunks
$\approx 790$\,ms \quad
$\delta^*{=}2$: 14/20 subjects \quad $\delta^*{=}1$: 6/20 subjects} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]
    \centering
    \includegraphics[width=\linewidth]{lata/lata_all_subjects_results.png}
    \caption{
        LATA on all 20 SEED-DV subjects (100 epochs each).
        \textbf{Left:} Mean learned delay distribution
        $\bar{\mathbf{w}}$ with $\pm1$ SD error bars; peak at
        $\delta{=}2$ ($\sim$810\,ms).
        \textbf{Centre:} Expected delay $\mathbb{E}[\delta]$ per subject;
        red dashed line and shaded band show mean $\pm$ SD.
        All subjects cluster tightly around 790\,ms.
        \textbf{Right:} Histogram of peak delays across subjects;
        14/20 prefer $\delta{=}2$, 6/20 prefer $\delta{=}1$,
        none prefer $\delta{=}0$ or $\delta{=}3$.
    }
    \label{fig:lata-seeddv}
\end{figure}

\paragraph{Discussion.}
The convergence of 70\% of subjects to $\delta^*{=}2$ and the remaining
30\% to $\delta^*{=}1$ — with no subject at $\delta{=}0$ or
$\delta{=}3$ — provides strong evidence that LATA is recovering a
genuine biological signal rather than fitting noise.
The preferred delay window of 500--1000\,ms is consistent with late
positive ERP components (P300, N400, late positive complex) that are
well-established correlates of higher-level semantic
processing~\cite{luck2014introduction}.
The near-identical distributions across subjects (SD of $\mathbb{E}[\delta]$
is only 25\,ms) further confirm that the learned latency reflects a
population-level property of the visual EEG response, not subject-specific
idiosyncrasies.

The bimodal split between $\delta{=}1$ and $\delta{=}2$ is itself
neuroscientifically interpretable: subjects whose EEG has higher
signal-to-noise ratio for earlier components (e.g.\ stronger P300 at
$\sim$300\,ms) may be captured by $\delta{=}1$, while those with more
prominent late positive complex activity at $\sim$600--800\,ms are captured
by $\delta{=}2$.
Future work with finer chunk resolution (e.g.\ 0.1\,s instead of 0.5\,s)
could resolve these components separately.

Taken together, the synthetic validation (Section~\ref{sec:lata-synthetic})
proves LATA reliably recovers known temporal offsets under controlled
conditions, while the 20-subject SEED-DV experiment demonstrates that the
same module — applied without modification across subjects — extracts a
consistent, biologically plausible latency estimate with no supervision
on the delay.

% ─────────────────────────────────────────────────────────────
\section*{References}
% ─────────────────────────────────────────────────────────────

\begin{thebibliography}{9}

\bibitem{luck2014introduction}
S.~J. Luck.
\newblock \emph{An Introduction to the Event-Related Potential Technique}.
\newblock MIT Press, 2nd edition, 2014.

\bibitem{tong2022videomae}
Z.~Tong, Y.~Song, J.~Wang, and L.~Wang.
\newblock VideoMAE: Masked autoencoders are data-efficient learners for
  self-supervised video pre-training.
\newblock \emph{Advances in Neural Information Processing Systems}, 35,
  2022.

\bibitem{liu2024eeg2video}
X.-H. Liu, Y.-K. Liu, Y.~Wang, K.~Ren, H.~Shi, Z.~Wang, D.~Li, B.-L. Lu,
  and W.-L. Zheng.
\newblock EEG2Video: Towards decoding dynamic visual perception from EEG
  signals.
\newblock \emph{Advances in Neural Information Processing Systems}, 37,
  2024.

\bibitem{radford2021clip}
A.~Radford, J.~W. Kim, C.~Hallacy, A.~Ramesh, G.~Goh, S.~Agarwal,
  G.~Sastry, A.~Askell, P.~Mishkin, J.~Clark, G.~Krueger, and I.~Sutskever.
\newblock Learning transferable visual models from natural language
  supervision.
\newblock In \emph{Proceedings of ICML}, 2021.

\end{thebibliography}

\end{document}
